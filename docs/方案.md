YES为保留，NO为消除

| 成就 | 当前解锁方式 | 解锁时附带效果 | 状态 |
| --- | --- | --- | --- |
| Konami Code | 依次按 `↑ ↑ ↓ ↓ ← → ← → B A`，相邻按键不超过约 0.8 秒 | 粒子爆发；角色尝试跳跃 | 可用 |NO
| 显示桌面 | 按 `Ctrl + D`（macOS 为 `Command + D`） | 最小化全部窗口 | 可用；可能与浏览器书签快捷键冲突 |no
| 星空占卜 | 按 `Ctrl + Shift + N`，或在终端输入 `star` | 弹出随机占卜卡片 | 可用；可能与浏览器无痕窗口快捷键冲突 |yes
| 沉浸模式 | 按 `F11`，或从开始菜单进入沉浸模式 | 切换网页全屏 | 可用；部分浏览器会优先处理 F11 |yes
| 命令行 | 按 `Ctrl + K`，或从开始菜单打开终端 | 打开站内终端 | 可用 |yes
| 粒子漩涡 | 设计意图是触发桌面粒子漩涡 | 无 | **待修复：监听了未被发送的事件，当前不能解锁** |NO
| 星座聚集 | 设计意图是双击桌面，使粒子组成星座 | 粒子短暂聚集 | **待修复：动画可触发，但没有写入成就记录** |NO
| 别点啦！ | 1.5 秒内连续点击角色 5 次 | 角色生气并短暂转身 | 可用 |YEs
| 窗口抖动 | 在约 0.5 秒内左右快速拖动窗口，形成至少 4 次明显反向 | 最小化全部窗口 | 可用，但触发手感偏严格 |NO
| 深夜模式 | 网页在本地时间 `00:00:00–00:00:04` 之间保持打开 | 无 | 可用，但触发窗口很窄 |NO
| 休息提醒 | 单次页面会话持续 1 小时 | 角色弹出休息提醒 | 可用 |NO
| 猫咪协议 | 在终端输入 `summon cat` | 像素猫在桌面游走约 10 秒 | 可用 |YES
各种信号合为一个
| 光谱收集者 | 在同一轮里点亮当时生成的全部彩色信号 | 无 | 可用；一轮只会随机生成 6–8 个信号 |NO

| 电子 | 阶段 | 建议说明 |
| --- | --- | --- |
| HTML | 主要使用 | 搭建网页结构与内容层级 |
| CSS | 主要使用 | 页面视觉、动效与响应式布局 |
| JavaScript | 主要使用 | 网页交互、状态与界面逻辑 |
| Python | 实践过 | 用于脚本、自动化或小型工具；需本人确认实际项目 |
| Node.js | 实践过 | 用于本地开发环境或简单服务；需本人确认是否写过接口 |
| Godot | 实践过 | 制作游戏原型与交互实验 |
| Git | 实践过 | 个人版本记录；若没有多人协作经历，不写“协作习惯” |NO C语言
| TypeScript | 学习中 | 正在了解类型系统与前端工程化 |NO              electron
| C++ | 学习中 | 正在学习语法、算法或基础概念 |NO                    汇编入门
| Rust | 学习中 | 正在探索所有权与系统编程概念 |NO



不再显示“HTML 80%”这类无法验证的数值，改成三组：

- **主要使用**：HTML、CSS、JavaScript
- **项目中使用过**：Python、Node.js、Godot、Git
- **正在学习**：TypeScript、C++、Rust

如果某项与你的实际情况不符，应调整分组，而不是调整一个看似精确的百分比。YES

## 后续优化备忘（暂不在本轮展开）

- 媒体记忆库仍有 `6 部电影、1 本书、1 部剧集` 的旧分类数据。主界面只保留“游戏 / 番剧”后，需要决定这些记录是归档、迁移还是移除，避免统计与分类口径不一致。
- 移动端采用独立交互构图，不再把桌面端等比例压缩：导航、角色占位、窗口阅读层和底部信息区分别重排。
- 本轮先完成“关于本站”的 V1.0 / V2.0 叙事、VRMA Lab 动作流程和第三方模型署名；上线前必须完成模型与动作资源的逐项许可复核。
