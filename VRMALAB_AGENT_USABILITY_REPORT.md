# VRMAlab 当前状态与 Agent 可用性报告

日期：2026-08-03  
测试对象：`shokuhou.vrm` + `X Bot@Clapping.fbx`  
测试环境：`http://localhost:3000/`

## 1. 本次交付

- 已导出调整后的 VRMA：`exports/shokuhou-clapping-agent-adjusted.vrma`
- 文件大小：73,644 bytes
- SHA-256：`E3D861FFF80B4D28D72E58556824AC54D6351662C4A54DE8DBD040047885CAE8`
- VRMA 结构回读：通过
- VRMA 语义容差比较：通过
- 骨骼编辑与根运动编辑：完整保留
- 舞台镜头：VRMA 格式不支持，导出时省略；这属于格式限制，不是动画数据丢失

本次调整使用两个连续片段：

1. `0.00–1.00s`：主调整片段
2. `1.00–1.17s`：收尾补偿片段，防止动作结束前表情与上身姿态突然复原

两个片段使用相同参数：

- 头部：低头 `-6`、左转 `-3`、左倾 `-4`
- 上胸部：前倾 `-4`、右转 `2`、左倾 `-2`
- 表情：开心 `48%`、放松 `14%`

## 2. 已确认的问题

### P0：WebM 录制功能当前不可用

录制能力检查显示：

- MediaRecorder：可用
- VP9 WebM：可用
- 舞台 `captureStream`：可用
- 统一帧渲染器桥接：缺失

因此软件拒绝生成 WebM。这个行为比输出损坏视频更安全，但录制入口目前实际上不可交付。应优先接通统一帧计算器与录制管线，并加入最小自动化测试：加载模型和动作、录制两秒、验证 WebM 非空且帧数增长。

### P1：调整片段固定为一秒，无法自然覆盖任意长度的动作

本次源动画长 `1.167s`，主片段只能覆盖前一秒，最后 `0.167s` 会突然恢复原始头部与表情。为了避免跳变，只能额外创建第二片段并重复全部参数。

建议增加：

- “覆盖整个动作”模式
- 可编辑的开始时间和结束时间
- 将片段延伸到动作末尾
- 复制、粘贴、克隆片段
- 相邻片段自动继承上一片段参数

### P1：视线参数的键盘与自动化输入路径不能可靠写入

本次测试中，视线数值框在填写并确认后会恢复为 `0`；对滑杆发送方向键也没有产生可验证变化。鼠标拖动路径尚未证明存在同样问题，因此应暂时定义为“键盘与自动化兼容性缺陷”。

建议让滑杆、数值框和内部状态使用同一个受控数据源，并为 `input`、`change`、键盘输入分别增加测试。

### P1：导出完成但没有产生标准、可监听的下载事件

界面明确显示 `Export completed`，文件也实际出现在下载目录；但浏览器自动化等待标准下载事件时超时。对人工用户影响很小，对 Agent 和自动化测试影响很大，因为它们无法可靠知道文件名、落盘位置和校验结果。

建议导出完成后在界面和程序接口同时返回：

- 文件名
- MIME 类型
- 文件大小
- SHA-256
- 导出状态 ID
- 明确的可下载链接或 Blob URL

### P2：项目上下文命名容易误导

导入食蜂操祈模型和鼓掌动作后，项目仍显示为 `Supplied Assets Project`，资源库还保留另一个 `X Bot@Thinking.fbx`。当前动作虽然有选中状态，但 Agent 必须阅读一长串名称与哈希才能确认导出对象。

建议自动根据当前模型和动作生成项目名，并提供“仅显示当前资源”视图。

### P2：动作末端缺少连续性诊断

系统没有主动提示片段只覆盖到 `1.00s`、而动作仍持续到 `1.167s`。如果不逐帧检查，用户很容易导出带有最后一帧跳变的动画。

建议在时间线上显示未覆盖区，并在导出前报告：

- 未覆盖的时间范围
- 参数突变位置
- 相邻片段的姿态差值
- 首尾循环误差

### P2：缺少更细的重定向修正能力

当前全局骨骼偏移可以改善整体姿态，但不能解决所有 FBX→VRM 问题，例如手掌接触、手指穿插、肩肘链条比例差异和不同体型造成的接触点漂移。

建议后续增加：

- 手部接触目标与简单 IK
- 左右手间距限制
- 肩、肘、腕的链式修正
- 手指预设
- 脚底锁定与根运动可视化

## 3. Agent 使用时的主要不便

### 3.1 缺少稳定的控件身份

页面中存在多个同名按钮，例如“添加调整片段”和文件选择入口。许多按钮主要靠图标或长文本区分，没有稳定的 `data-testid`、资源 ID 或片段 ID。Agent 必须反复读取整页结构，再通过位置和上下文排除歧义。

建议所有可操作对象提供稳定标识，例如：

```text
data-testid="add-adjustment-clip"
data-clip-id="clip-001"
data-bone="head"
data-expression="happy"
data-resource-id="..."
```

### 3.2 时间轴只能依靠视觉坐标定位

播放头没有可直接填写的“当前时间”输入框，也没有语义明确的滑杆。Agent 想跳到 `1.00s` 时只能根据时间线宽度估算点击坐标，这在不同分辨率、缩放和面板宽度下不稳定。

建议增加：

- 可填写的播放头时间
- “跳到片段开始/结束”按钮
- “跳到动作末尾”按钮
- 时间轴滑杆的标准 ARIA 值

### 3.3 每次状态更新后需要重新定位控件

调整一个参数会触发属性面板重新渲染。自动化工具持有的旧控件引用可能过期，所以批量设置三个旋转轴时必须逐项重新查找。人工用户不明显，但会让 Agent 操作变慢、容易出现部分参数未提交。

建议保持控件节点身份稳定，或提供一次性提交一组参数的接口。

### 3.4 缺少批量编辑与参数复制

为了修复最后 `0.167s`，Agent 必须重新添加头部、上胸部和表情，再逐项复制数值。软件没有“复制上一片段”“应用到所选片段”或“批量粘贴参数”。

### 3.5 导出流程对自动化过于对话框化

导出需要打开对话框、选择格式、阅读能力报告、勾选确认、点击导出，再从外部下载目录核实文件。人类可以接受，但 Agent 缺少一个确定性的结果对象。

### 3.6 缺少机器可读的诊断结果

能力报告主要是面向人的文本。Agent 更需要结构化数据，例如：

```json
{
  "ready": true,
  "format": "vrma",
  "preserved": ["boneEdits", "rootMotion"],
  "omitted": ["stageCamera"],
  "validation": {
    "structuralRoundTrip": "passed",
    "semanticTolerance": "passed"
  }
}
```

## 4. 建议增加“Agent 模式”

不建议把 Agent 模式做成另一套复杂界面。更合适的做法是：保留现有可视化工作台，同时为它增加稳定、可审计的操作层。

### 第一阶段：让现有界面可可靠自动化

1. 为所有关键控件添加稳定 ID 和完整 ARIA 标签。
2. 增加数值播放头、片段开始/结束字段。
3. 修复滑杆的键盘和程序化输入一致性。
4. 导出成功后显示文件名、大小和哈希。
5. 修复 WebM 统一帧渲染器桥接。
6. 增加“复制片段”“延伸到末尾”“覆盖整个动作”。

### 第二阶段：增加本地 Agent 操作协议

提供只监听本机的结构化接口，例如：

```text
GET  /agent/project
GET  /agent/selection
POST /agent/playhead
POST /agent/clips
PATCH /agent/clips/{id}
POST /agent/validate
POST /agent/export/vrma
GET  /agent/jobs/{id}
```

接口应与界面共用同一个状态管理层，任何 Agent 操作都立即反映到 UI；用户仍能实时看到、撤销和审核。

### 第三阶段：事务化与可审计操作

Agent 每次修改先进入草稿事务：

1. Agent 提交一组修改。
2. 软件返回修改前后差异、受影响时间范围和验证结果。
3. UI 显示临时预览。
4. 用户可以接受、撤销或要求继续调整。
5. 所有操作写入可重放日志。

推荐操作记录格式：

```json
{
  "operation": "update_adjustment_clip",
  "clipId": "clip-001",
  "changes": {
    "bones.head.pitch": -6,
    "expressions.happy": 48
  },
  "reason": "reduce rigid neutral appearance during applause"
}
```

## 5. 推荐优先级

1. 修复 WebM 录制桥接。
2. 支持任意长度片段、延伸到动作末尾和复制片段。
3. 修复视线/滑杆的键盘与自动化输入。
4. 为关键控件增加稳定 ID，并增加数值播放头。
5. 让导出返回可机器验证的文件信息。
6. 再建设本地 Agent API、事务预览和操作日志。

## 6. 结论

VRMAlab 已经具备可用的核心链路：模型与 FBX 导入、重定向预览、骨骼和表情调整、A/B 对比、验证与 VRMA 导出。本次 VRMA 也成功通过结构和语义验证。

当前限制并不是“Agent 完全不能使用”，而是 Agent 必须像人一样反复读界面、按坐标操作、手工复制参数，并在导出后自行到文件系统核验结果。只要先补齐稳定控件身份、数值时间轴、批量编辑、确定性导出和录制桥接，软件就能从“Agent 勉强能操作”提升到“Agent 可以可靠、高效且可审计地操作”。
